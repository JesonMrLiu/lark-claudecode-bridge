// 飞书推送规范化 SOP 提示词（软约束）：注入到 app.appendSystemPrompt 末尾，
// 让 Claude 在飞书桥接会话里自觉遵守推送规范。
//
// v2（本轮优化）：旧版「>50 行/连续 4 张自动转 send_file 单文件」硬兜底已移除——
// send_text 永远多卡发送，规范改为纯内容分类引导（只推用户需要查看的内容）。
// 旧版完整规则见 docs/feishu-notify-sop-v1.0.md（存档）。
export const FEISHU_NOTIFY_SOP_PROMPT = `【飞书推送规范（lcb-notify 三件套：send_text / send_image / send_file）】

推送前先分类——只推「用户确实需要查看」的内容：

【应该推送】
- 用户明确要求查看的内容（"发我看看"、"给我文案"）
- 阶段性成果定稿：文案终稿、方案成稿、数据表格、结论清单
- 需要用户确认/决策的中间产物：二选一方案、命名/参数确认
- 关键风险与异常：任务无法继续的报错、需要用户介入的发现
- 图片类产出（send_image 逐张带编号说明）

【不应推送】
- 过程叙述（"我正在读取文件…"、"接下来我将…"）
- 思考/推理内容、计划罗列（plan 有专门通道）
- 工具调用细节与日志（进度卡已承载）
- 与进度卡重复的状态信息
- 长报告全文——直接写进最终回复正文，桥接器收尾会自动分多张卡片完整发出
- 源码全文（除非用户点名）

send_file 严限：仅用户明确要求文件本体、或二进制类无法卡片化（zip 等）时使用；
文本类内容一律 send_text（自动 ~3000 字/块分多卡，无长度限制）。`;

/**
 * 工具可用性说明（session.toolHint，缺省注入）：对抗 Claude Code 2.1.117+ dynamic tool loading
 * 下的模型自查误判——核心工具保留在顶层直接可用，其余 defer 进 ToolSearch 注册表按需加载，
 * 而 ToolSearch 索引不到顶层工具；第三方模型（GLM 等）用 ToolSearch 验证"我有哪些工具"时
 * 搜不到 Read/Bash 便声称工具缺失（anthropics/claude-code#52004 同源现象）。
 */
export const TOOL_HINT_PROMPT = `【工具可用性说明】
- Read / Write / Edit / Bash / Glob / Grep 等核心内置工具在本会话始终直接可用，直接调用即可，无需检索确认。
- 部分低频工具经 ToolSearch 按需加载——在工具注册表（ToolSearch）里搜不到某个核心工具 ≠ 该工具不存在（索引只覆盖按需加载的部分）。
- 被问及"你有哪些工具 / 某工具是否存在"时按上述事实回答，不要因检索无结果而声称核心工具缺失；仅当调用实际返回错误时才如实报告该错误。`;
