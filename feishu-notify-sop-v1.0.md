# 飞书推送规范化方案（v1.0）

> 目标：让 Claude 在飞书桥接会话中的消息推送可读、可追溯、不刷屏。
> 适用范围：所有 `lcb-notify` 系列工具（`send_text` / `send_file` / `send_image`）。

---

## 一、决策规则（发送前必查）

| 场景 | 数量阈值 | 推荐方式 |
|------|---------|---------|
| 主卡片正文 | — | ≤ 10 行 |
| 关键改动文件 | 1–10 个 | `send_file` 单发，每张卡片附一句话说明 |
| 辅助改动文件 | 10–30 个 | 合并为 1 份 Markdown 再 `send_file` |
| 任意场景文件 | > 30 个 | 不发送，主卡片只列本地路径 |
| 源码全文 | 不论多少 | 不发送（除非用户明确点名） |
| plan 文件 | 1 份 | 不推送，仅本地落盘于 `~/.lark-claudecode-bridge/claude/plans/` |
| 子代理进度 | — | 子代理卡片，不在主卡片复述 |
| 合并 Markdown | — | 文件名 `changes-<任务简称>-<日期>.md` |

---

## 二、主卡片文案模板（固定 4 段）

```markdown
📌 阶段：{探索 / 设计 / 编码 / 联调 / 验收}

✅ 已完成
- {条目 1}
- {条目 2}

🔍 关键发现（如有）
- {一句话，不附代码引用}

📂 下一步
- {具体动作，可被执行}
```

**硬约束**：超 10 行立刻拆——`send_text` 第二张卡片接续，或改 `send_file` 发附件。

---

## 三、关键改动文件 → 合并报告模板

文件名约定：`changes-<任务简称>-<日期>.md`

```markdown
# 改动报告：{任务名}

> 生成时间：{ISO 时间}
> 涉及模块：{模块名}

## 一、改动清单

### 新增（n 个）
- `path/to/A.java` — 一句话说明
- `path/to/B.java` — 一句话说明

### 修改（n 个）
- `path/to/C.java` 行 123-145 — 一句话说明
- `path/to/D.java` 行 67-80 — 一句话说明

## 二、关键代码片段（仅核心逻辑）

```java
// C.java L123-145（核心：xxx 校验）
public void update(...) { ... }
```

## 三、验证步骤

1. {可执行命令或 URL}
2. {预期结果}
```

调用方式：实施完成后 `send_file` 这一份报告即可。

---

## 四、子代理 prompt 模板（在 system / agent prompt 里固化）

### Explore agent

```
回报硬约束：
- 结论 ≤ 100 字，只列路径和结论
- 禁止大段代码引用
- 禁止重复文件内容
- 表格化呈现：文件路径 / 行号 / 结论三列
- 总字数 ≤ 500 字
```

### Plan agent

```
回报硬约束：
- 只给实施步骤 + 关键文件路径
- 禁止贴代码块
- 禁止重述探索结论
- 总字数 ≤ 400 字
- 完整方案写到 plan 文件，不在回报里复述
```

---

## 五、禁止清单（违反即触发自我纠正）

| 禁止行为 | 替代方案 |
|---------|---------|
| 主卡片内联 ≥ 30 行分析 | 拆卡片或改附件 |
| `send_text` 整段 Explore 报告 | Explore 回报走子代理卡片，主卡片只引用 1 行结论 |
| `send_file` 源码全文 | 不发 |
| `send_file` > 10 个文件 | 合并为 1 份 Markdown |
| 主卡片复述 plan 文件内容 | plan 文件本地落盘即可，不推送 |
| `send_text` 后再 `ExitPlanMode` | 二选一：要么正文说话，要么按钮交互 |
| 主卡片连续推 ≥ 3 张 `send_text` | 改写一份 Markdown `send_file` |

---

## 六、API 调用顺序（典型场景）

### 场景 A：常规开发任务

```
1. 子代理卡片：3 个 Explore 并行（自动产生）
2. 子代理卡片：Plan agent（自动产生）
3. 主卡片：AskUserQuestion（澄清）
4. 主卡片：收到答案后简短确认（≤ 3 行）
5. ExitPlanMode（仅按钮，无文本推送）
6. 子代理卡片：各实施步骤
7. 主卡片：完工 → send_file 一份 changes 报告
```

### 场景 B：紧急线上排查

```
1. 主卡片：📌 进入排查模式 + 待排查现象（≤ 3 行）
2. 子代理卡片：证据采集（log-fetcher / archery / codegraph）
3. 主卡片：📌 根因 + 修复建议（≤ 5 行）
4. （如需修复）→ 走场景 A 流程
```

### 场景 C：代码审查

```
1. 主卡片：📌 进入审查模式 + 审查目标（≤ 3 行）
2. 子代理卡片：4 个 reviewer 并行
3. 主卡片：📌 审查结论分级摘要（≤ 8 行）
4. （如需修复）→ send_file 一份 findings 报告
```

---

## 七、自我纠正触发条件

当 Claude 检测到违反以上规则时，立即在当前卡片追加：

```markdown
⚠️ 推送上限触发：{触发原因}
   改用：{附件 / 拆卡片 / 列路径}
```

并停止继续 `send_text`。

---

## 八、执行优先级

| 优先级 | 项 | 落地时机 |
|--------|-----|---------|
| P0 | 主卡片 ≤ 10 行硬约束 | 立即生效 |
| P0 | plan 文件不主动 push | 立即生效 |
| P1 | 子代理回报模板化（≤500 字） | 立即生效 |
| P1 | changes 报告模板 | 立即生效 |
| P2 | send_file 合并策略 | 立即生效 |
| P2 | 场景 A / B / C 流程 | 立即生效 |
| P3 | 写入项目根 `AGENTS.md` 备忘 | 下次会话开始前 |

---

## 九、版本与维护

- 当前版本：v1.0
- 维护人：Claude（d2c-V3 项目会话）
- 修订触发：用户明确反馈"卡片太乱 / 文件太多 / 找不到上下文"
- 升级路径：当规则约束过紧影响任务推进时，提交 v1.1 修订申请