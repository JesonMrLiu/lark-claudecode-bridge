# 2026-09-09：lark-claudecode-bridge 12 项问题大改造完成

## 改动总览（前后跨度两会话，前一会话进展大半；本次补完 #11 + 测试）

### 已落地（1-12）
1. **首装目录初始化** ✅ `src/util/runtime-dirs.ts` + `lcb/bin.ts main()` + `startWebServer()` 入口调用
2. **/new 清排队 + 取消汇总** ✅ ChannelRuntime 加 epoch；/new 时 epoch++、abort 任务、settle 挂起 confirm/plan/qa，回「🗑️ 已因新建会话取消 N 条排队消息」
3. **问答嵌入主卡片** ✅ `ProgressCard.setQuestion` + `updateQuestionAnswer`（去独立卡片）
4. **计划嵌入主卡片 + 直接发 md 文件** ✅ `planAsk` 落盘 `~/.lark-claudecode-bridge/claude/plans/<ts>-<slug>.md`；新决策 `plan-view-file` 直接 send_file（feishuGateway 走 file_type:stream）
5. **code-dev 主卡片正文折叠** ✅ `TAIL_LIMIT_CODE_DEV = 400`（vs generic 1200）
6. **回复链上游消息随 prompt** ✅ SDK 不暴露 im.message.get，用 `client.request({ method: 'GET', url: '/open-apis/im/v1/messages/:id' })`；最多 3 层
7. **日志按天拆 logs/bridge-YYYY-MM-DD.log** ✅ `src/util/log-tee.ts` + 14 天清理 + 移除 bridge.log 重定向
8. **卡片宽度可配置** ✅ `CardConfig.width = default|fill` + `card-builder.ts` 输出 `config.width_mode: fill` + 热生效
9. **session 配置项上页** ✅ `PUT_SECTIONS` 加 'session'，workspace 页加 `context_remind_tokens` 输入
10. **工作区删按钮修复** ✅ `tr.querySelector('.danger')` 精确选择
11. **飞书推送规范化 SOP** ✅ 软约束 `appendSystemPrompt += FEISHU_NOTIFY_SOP_PROMPT` + 硬兜底 `createGatewaySender({sopOptions})` sendText >50 行 / 连续 ≥4 张自动转 changes-*.md + send_file
12. **Skills/MCP 管理页** ✅ 后端 `skills-mcp-api.ts` 4 个 API（user 级 `.claude.json`）+ 前端 `pages/skills-mcp.js`

### 关键决策
- **planAsk/askQuestion 不再独立发卡**：嵌入主进度卡的 inner 区域（#3+#6 一并实现），跟用户「彻底替换」要求一致
- **plan-view-file 直接 send_file**：不再塞卡片正文，避免 30KB 上限
- **/new 清 epoch + abort + settle**：队列不驻留旧代际消息，用户可见「已取消 N 条」汇总
- **bridge 暴露 `_pendingAsk(key)`** 诊断接口，给测试用（生产期不影响）
- **qa 答案更新单一权威源**：ProgressCard 唯一维护；index.ts qa-pick 不再 mutate qa.answers（之前双 toggle bug）

### 测试
全部 514 测试通过（包含因新行为重写的 6 个 wiring 用例 + 新增 planAsk 嵌入主卡路径用例）

### 文件变更摘要
新增：`src/util/runtime-dirs.ts`、`src/util/log-tee.ts`、`src/notify-sop.ts`、`src/web/skills-mcp-api.ts`、`assets/web/js/pages/skills-mcp.js`
修改：`src/index.ts`（channel epoch + _pendingAsk + SOP 注入 + sopOptions + qa-pick 单一权威源）
修改：`src/executor/notify-server.ts`（sopOptions + 硬兜底落 notify/）
修改：`src/gateway/{feishu-gateway,card-builder,progress-card}`（parentId 回复链 / workspaceType 折叠 / planAsk+qaAsk 嵌入 API）
修改：`src/{types,config,bin/lcb,web/{config-api,server,lifecycle}}`（CardConfig / SessionConfig / 首装目录 / 启动日志 tee）
修改：`assets/web/js/pages/workspaces.js`（删按钮精确选择 + context_remind_tokens + card.width 输入）
修改：`tests/wiring.test.ts`（6 个用例适配嵌入主卡路径 + 新增 planAsk 嵌入主卡用例）
